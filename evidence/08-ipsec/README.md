# Lab 8 evidence

This directory is intentionally included so the automation can write all
baseline, XFRM, connectivity, fail-closed, firewall, service and packet-capture
proof into one isolated location.

Run:

```powershell
.\scripts\lab8\Invoke-CSA-Lab8.ps1 -Phase All
```
